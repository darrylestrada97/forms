import { CompareCountriesDirective } from './compare-countries.directive';

describe('CompareCountriesDirective', () => {
  it('should create an instance', () => {
    const directive = new CompareCountriesDirective();
    expect(directive).toBeTruthy();
  });
});
