import { ImprovementsDirective } from './improvements.directive';

describe('ImprovementsDirective', () => {
  it('should create an instance', () => {
    const directive = new ImprovementsDirective();
    expect(directive).toBeTruthy();
  });
});
