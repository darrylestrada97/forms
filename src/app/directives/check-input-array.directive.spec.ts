import { CheckInputArrayDirective } from './check-input-array.directive';

describe('CheckInputArrayDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckInputArrayDirective();
    expect(directive).toBeTruthy();
  });
});
